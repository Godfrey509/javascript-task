// perform addition
const addition = (x, y) => {
  sum = x + y;
  console.log(sum);
};

// perform subtraction
const subtraction = (x, y) => {
  sum = x - y;
  console.log(sum);
};

// perform multiplication
const multiplication = (x, y) => {
  sum = x * y;
  console.log(sum);
};

// perform division
const division = (x, y) => {
  sum = x / y;
  console.log(sum);
};

//
addition(22, 18);
subtraction(164, 119);
multiplication(12, 7);
division(2, 4);
